import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.bembatranslate.app",
  appName: "BembaTranslate",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
